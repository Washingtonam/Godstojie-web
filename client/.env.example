# Frontend API base URL for Render backend
VITE_API_URL=https://your-render-backend.example.com

# Make sure the backend is configured with ADMIN_USERNAME and ADMIN_PASSWORD to sign in to the admin dashboard.
